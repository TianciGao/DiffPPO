# v0.1.0 — PPO-DAP clean-room theory-v6

Published: 2026-08-20

Release: https://github.com/TianciGao/DiffPPO/releases/tag/v0.1.0

`v0.1.0` is the first public release of the independent clean-room PPO-DAP theory-core implementation based on [arXiv:2409.01427v6](https://arxiv.org/abs/2409.01427v6).

## Included

- the `ppo_dap` theory-core library;
- the complete clean-room validation suite;
- locked Python 3.12 / Torch package metadata;
- MIT license;
- theory-conformance documentation;
- byte-level source provenance.

The public release is derived from the audited source authority `TianciGao/DPPO` at annotated tag `theory-v6-local-ready` (`64337ea47e0939a1f019b6c21ef5d35641a42e4c`).

## Validation

The public release candidate was independently checked from a fresh clone and locked environment. Validation covered:

- locked dependency installation;
- wheel and sdist build;
- clean wheel installation;
- import smoke test;
- full test suite;
- public-tree/source provenance;
- README command flow;
- privacy and artifact-scope review.

The complete suite finished with:

```text
350 passed / 0 failed
```

The release merge commit has the exact same tree as the validated candidate, so the validation evidence transfers mechanically to the published release tree.

The GitHub Release contains the validated wheel, sdist, checksum file, R2 validation manifest and R2 reconciliation manifest.

## Scope

This is a **library/theory-core release**. It does not include a paper-scale MuJoCo experiment harness, benchmark configuration or trained-model bundle.

Algorithm implementation and theory conformance are complete for the audited theory scope. Empirical reproduction is separate and remains incomplete.

## Claim boundary

This release does not claim reproduction of the paper's rewards, learning curves, runtime, training duration, GPU behavior or benchmark results.

The detailed audited theory boundaries are maintained in [../THEORY_CONFORMANCE.md](../THEORY_CONFORMANCE.md).

## Legacy repository history

The pre-clean-room public implementation is preserved in repository history and under the annotated tag:

`legacy-pre-cleanroom-main`

No historical branch or commit was rewritten to publish `v0.1.0`.
